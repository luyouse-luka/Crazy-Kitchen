# 疯狂后厨 —— 开发执行方案 v1.3

> 2026-08-07 一天四版：首版 → 引擎与视角拍板（重算性能账）→ 重排里程碑与开发流程 → **本版：玩法基准**。
>
> **本文件是 v1.3 的项目内正本**（2026-08-07 落盘）。后续所有修订以本文件为准。
> `~/.claude/plans/twinkling-orbiting-sparkle.md` 是落盘底本，**不再维护**——那是会话临时区，换会话就可能被覆盖。

### 📄 `docs/workflow-plan.html` —— 本文件的执行视图（2026-08-08 加）

**日常开工看那个，不是看这个。** 本文件 66KB、十一节，是完整方案，找不到重点；
`docs/workflow-plan.html` 是它的操作面板：双泳道（左边你、右边我）排出 M0→M7 每阶段谁做什么、
交接点在哪、闸门横跨两道。clone 下来**双击就能用浏览器打开**，不需要任何依赖。

里面额外展开了本文件没有的两块：**线 A 的 A2–A8 逐步操作 + 每步判据**，
以及一个只在实操中才暴露的坑 —— Cocos 新建项目要求空目录，会和已有的
`game/assets/logic/` 撞车，解法在 A2 那节。

- 源文件 = `docs/workflow-plan.html`，**改它就是改正本的执行视图**，别改别处的副本
- 已发布为在线页面：https://claude.ai/code/artifact/3dd1abc0-b66a-4749-94ca-bb4ffd527dd8
  （私有。要更新时改本地源文件后，用这个 URL 重新发布以保持链接不变）
- 两者冲突以**本文件为准** —— 执行视图是派生物，不是第二份真相源

**v1.3 新增「玩法基准」一节**，并在同一轮内推翻了它的初稿框架：

- 初稿按「胡闹厨房有哪些机制 → 我们抄几条」来排。**那个框架本身就会让本项目滑向
  「胡闹厨房的手机单人版」**，独有的东西被挤到边缘。
- 现在改为：**先立本项目自己的三根柱子，参考作降级为工具箱**，并加裁决规则——冲突时砍参考作。
- 连带收回一个说法：初稿写「AI 顾客顶替队友的功能位」，把原创主张说成了替补。
  实际因果是反的：**正因为单人，AI 顾客才做得起来**（多人塞不下 30 字台词）。

据此还发现并补上 **M4 缺失的「打烊后经营循环」**——上一版有天数推进却没有结算与升级，
天会推进到一个空房间。

<details>
<summary><b>v1.2 修了什么</b>（8 条上一版的实际缺陷，展开看）</summary>

| # | 上一版的问题 | 现在 |
|---|---|---|
| 1 | `logic/` 与 `game/` 平级——**Cocos 只编译 `assets/` 下的脚本，M0 就会撞墙** | 移进 `game/assets/logic/`（§3.1） |
| 2 | AI 顾客验证排在第 13 周，但 `GDD.md` §11 说它是「最重要的问题」 | 提到**第 1 周**，作为 go/no-go 闸门（M0 线 C） |
| 3 | 「好不好玩」要等第 8–10 周（M3）才知道 | 新增 M1 无头压力原型，**第 3 周**就有答案 |
| 4 | 纯线性排期，但工作实际有三条互不阻塞的线 | 三线并行（§5.0） |
| 5 | M1 验收「能走能拿能放」——不可玩，学不到东西 | 改成垂直切片：**30 秒能玩完一局** |
| 6 | 软著排在 M3，且游戏名「未定」——**这是个死锁** | 移到 M2 出口，游戏名在那里定死（软著名≠上架名会被拒） |
| 7 | 只有「我做/你做」一张表，没写怎么交接 | 新增第六节完整开发流程 |
| 8 | 无风险预案、无里程碑闸门 | 新增第八节 + §6.8 |

</details>

## 📍 当前断点（2026-09-02 第二轮 · 每轮收工更新这一段）

**发布链路整条通了，包体数字已刷新且不再过期。A7 的探针我写好了，
就差你在编辑器里挂一个组件 + 构建一次。剩下三件卡在你那边：A7 构建、
引擎插件开不开的决策、`ANTHROPIC_API_KEY`。**

**产物目录换了名**：`../wechatgame-001/` → **`../wechatgame/`**（Unison 传上来的），
`pnpm size` 的默认路径已跟着改，直接跑不带参数就行。

| | 状态 |
|---|---|
| **线 A（你）** | 🟡 A1–A5 完成。A6 你说完成了，但产物里查不到 `iOSHighPerformance`（见下）；**A7 探针已就位，等你构建** |
| **线 B（我）** | ✅ B1–B7 全部完成 |
| **线 C（我）** | ✅ C1 完成。⛔ **C2 仍卡在 `ANTHROPIC_API_KEY`** —— go/no-go 闸门，200 张卡约 $2.5 |
| **M1（我）** | ✅ 全部完成，数字见 M1 那一节 |

### 线 A 逐步核对（2026-09-02 从新构建产物反查，不是听说）

| 步 | 状态 | 判据实测 |
|---|---|---|
| A1 repo + remote | ✅ | `origin` = `git@github-luyouse-user:luyouse-luka/Crazy-Kitchen.git` |
| A2 工程与 logic 合到一处 | ✅ 服务器端已合（`24a6c36`） | ⚠ **你那边换工作副本还没做**，见 §A2-fix 末尾。判据：新产物里 `grep A7_LOGIC` / logic 字面量全部零命中，说明你构建的那份工程里还没有 `logic/` |
| A3 Cocos 3.8.x | ✅ | 3.8.8 |
| A4 构建 → 真机 | ✅ | `deviceOrientation: landscapeRight` ✓ · 设计分辨率 1280×720 ✓ |
| **A5 包体真数** | ✅ **已刷新，不再过期** | **主包 2428.0 KB**（红线 4096）· `cocos-js` **1844.3 KB 单体、0 插件** · `assets` **397.2 KB**（850 KB 天空盒没了）· 孤儿 0。详见 §2.4a |
| A6 AppID + 高性能 | 🟡 **两半都做了，等下次构建验证** | AppID `wx40db4a5ae317a9a5` 已进 `project.config.json` ✓ · 公众平台后台已开通生产提效包（你 09-02 确认）✓ · `game/build-templates/wechatgame/game.json` 模板已放 ✓。**判据：下次构建后产物 `game.json` 里要出现 `"iOSHighPerformance": true`** —— 没出现说明 build-templates 没被套用 |
| A7 分层落地验证 | 🟡 **第一次构建炸了，原因已修，等第二次** | 15:53 `buildScriptCommand failed with code -1` —— Cocos 把 `assets/` 下的 10 个测试文件也编译了，它们 import `vitest` / `ajv` / assets 外的 json。**已把 `__tests__/` 移到仓库根 `tests/`**，见 §3.1 那条。三件套 + 期望输出 + 三层判据在 **§A7** |

`pnpm check` 全绿：铁律① 0 命中 · typecheck 无错 · **131 个测试通过**。
`pnpm sim` 与 `pnpm a7` 在服务器上也都能跑了（修的是 `vite-node` 那个 tsconfig 坑，见 §A2-fix 补记）。

**本轮新增/改动的文件**（`project/kitchen-chaos/` 下）：

```
game/assets/scripts/a7-check.ts      ← 新增 · A7 探针内核，纯 TS 零 cc 依赖
game/assets/scripts/A7Probe.ts       ← 新增 · A7 Cocos 组件（挂场景用）
tools/a7-expect.ts                   ← 新增 · 同一份探针在 Node 跑，pnpm a7
tools/ensure-cocos-tsconfig.mjs      ← 新增 · 补 game/temp/tsconfig.cocos.json 占位
game/build-templates/wechatgame/game.json  ← 新增 · A6 的 iOSHighPerformance（全量覆盖，见 §2.7）
tests/                               ← 移动 · 从 game/assets/logic/__tests__ 搬出来，10 个测试 + fixtures
vitest.config.ts                     ← 改 · include 指向 tests/
tools/pkgsize.ts                     ← 改 · 默认路径 ../wechatgame；单体 cc.js 提示；私有配置不再误报孤儿
package.json                         ← 改 · 加 a7 / prea7 / presim
tsconfig.json                        ← 改 · include 收 a7-check.ts
ROADMAP.md                           ← 改 · 本段 + §2.4a 重写 + 新增 §A7 + §A2-fix 补记
```

**在此之前已落盘的**（累计）：

```
.gitignore · package.json · pnpm-workspace.yaml · tsconfig.json · vitest.config.ts
game/                                                 ← Cocos 3.8.8 工程（编辑器开这一层）
game/assets/logic/  types · vec2 · collision · recipe · order · rng · sim · difficulty · API.md
tests/  上述各一份 .test.ts + fixtures + schema + memory（10 个 · 131 测试）
tests/fixtures/customers.json                         ← 21 张手写顾客卡
pipeline/  dimensions.json · customer.schema.json · README.md
tools/sim-cli.ts · tools/pkgsize.ts                   ← pnpm sim / pnpm size
docs/workflow-plan.html                               ← 线 A 的执行视图
```

### ⚠ 需要你操作的事

| # | 事 | 状态 |
|---|---|---|
| 0 | **换工作副本**（合并的后半截，只有你能做） | ⬜ 见 §A2-fix 末尾。**A7 的前提** |
| 1 | **push 到 GitHub** | 🟡 本地领先 `origin/main` **3 个提交**（`6efadb0` `24a6c36` `e0b89b8`）+ 本轮这批。远端无独有提交，工作区干净，是直进（fast-forward）。**等你说推我再推**。`origin/master` 合完已无用，可删 |
| 2 | Unison 两端 ignore | ✅ 生效 |
| 3 | **`ANTHROPIC_API_KEY`** | ⛔ **仍未设置 —— 全项目当前最贵的一项** |
| 4 | A6 `iOSHighPerformance` | 🟡 三处都齐了，**构建一次就能验**（和 A7 同一次构建） |
| 5 | **A7：pull → 挂组件 → 再构建一次** | 🟡 第一次构建的失败原因已修并推送，**先 `git pull` 再构建** |
| 6 | **决策：微信引擎插件开不开** | ⏸ 这次构建没走插件，主包多了约 1388 KB。见 §2.4a |

**为什么 3 排在工程任务前面**：线 A 卡住只是慢，C2 是方向闸门 —— 200 张卡约 $2.5，
如果 AI 生成的顾客卡撑不起「场景即敌人」那套玩法，整个 GDD 要回炉。**三个月的账，别拿两块五赌。**

**M1 最该记住的一条**：⚠ **M4 的「升级项·设备」别做成「多一个烤炉」** ——
实测烤炉不是瓶颈，跑腿才是，玩家感觉不到。做成「烤得更快」或「工位更近」才有效。

**`iOSHighPerformance` 已落地（2026-09-02）**：产物的 `game.json` 每次构建重新生成，
直接改会被覆盖，所以模板放在 `game/build-templates/wechatgame/game.json`。

⚠ **模板里必须写全量，不能只写 `iOSHighPerformance` 一行** —— build-templates 是在构建插件
生成完 `game.json` 之后**整个文件覆盖**上去的，只写一行会把 `deviceOrientation` 冲掉，
横屏直接没了。现在模板 = 产物原有的两个字段 + `iOSHighPerformance`，
`diff` 过一遍确认只多这一行。
**代价**：以后在构建面板改屏幕方向 / 超时，得同步改这个模板，否则模板会把面板设置钉死。

连带一个已经定死的约束：**纹理格式只能 ASTC**（高性能模式不支持 ETC/PVRTC），
这条反过来约束整条美术管线，M2 开始做资源前就得配好。

#### §A2-fix · 两条历史合并记录（2026-09-02 · 服务器端已执行 `24a6c36`）

合并前：远端有两个互不相干的分支。

| 分支 | 内容 | 结构 |
|---|---|---|
| `main` (`7f41f1c`) | 逻辑层 + 管线 + 文档，20+ 文件 | `game/assets/logic/` · `pipeline/` · `tools/` · `ROADMAP.md` |
| `master` (`c340de8`) | Cocos 工程，19 文件 | **工程根就是仓库根**：`assets/` · `settings/` · `package.json` |

`git merge-base` 返回空 —— **两边没有共同祖先**，是各自 `git init` 出来的两棵树。
方案 §3.1 要求的结构是「仓库根 = `main` 的样子，Cocos 工程放在 `game/` 子目录」，
所以要把 `master` 那 19 个文件挪进 `game/`，与已有的 `game/assets/logic/` 并存：

```
Crazy-Kitchen/
├── game/                    ← Cocos 工程（用编辑器打开的是这一层）
│   ├── assets/
│   │   ├── logic/           ← main 带来的 20 个 .ts（编辑器会给它们生成 .meta，必须 commit）
│   │   ├── test.scene
│   │   ├── material.mtl
│   │   ├── surface-effect.effect
│   │   └── terrain.terrain
│   ├── settings/
│   ├── package.json         ← name 建议从 NewProject 改成 kitchen-chaos
│   └── tsconfig.json
├── pipeline/ · tools/ · ROADMAP.md · GDD.md · docs/
```

`master` 只有一个提交、没有值得保的历史，所以直接取内容、不做 merge：

```bash
git checkout main
git archive origin/master | tar -x -C game/     # 19 个文件解到 game/，零路径冲突
git add game && git commit
```

**⚠ 合并当场炸出来的一个坑（记下来，换机器还会遇到）**

合完 10 个测试文件**全部失败**，`TSConfckParseError: failed to resolve
"extends":"./temp/tsconfig.cocos.json"`。原因：`game/tsconfig.json` extends
`./temp/tsconfig.cocos.json`，而 `temp/` 是**编辑器生成、被 `.gitignore` 排除**的目录 ——
你机器上有所以看不到，**任何新 clone 或 CI 都会中**。vite 为每个 `.ts` 向上找最近的
tsconfig，找到 `game/tsconfig.json` 就死在 extends 上。

修法在 `vitest.config.ts` 把 `tsconfigRaw` 钉死。**必须传字符串** ——
vite 7.3 `config.js:6070` 是 `if (typeof tsconfigRaw !== "string")` 才去读磁盘，
传对象照样会去找文件、照样炸（这一步我先写成对象，白试了一轮）。

```ts
esbuild: { tsconfigRaw: '{"compilerOptions":{"target":"es2020"}}' },
```

⚠ **2026-09-02 补记：这条只救得了 vitest，救不了 `vite-node`。**
`vite-node@6` 拉的是 `vite@8`（转译走 `vite:oxc`，不是 esbuild），
`esbuild.tsconfigRaw` 和 `oxc.tsconfigRaw` 都不起作用，
建 `vite.config.ts` 或 `-c` 显式指定也一样 —— 它照样去磁盘找 `temp/tsconfig.cocos.json`。
所以 `pnpm sim` 在服务器上一直是坏的（M1 那些数字是依赖升级之前跑的）。
现行修法是 `tools/ensure-cocos-tsconfig.mjs` 补一份最小占位，
由 `presim` / `prea7` 自动调起，**文件已存在就不写**，编辑器那份永远优先。

**两份 `.gitignore` 并存是对的，别合并**：根目录那份管 `game/library/`、`game/temp/` 等
（本来就是按「工程在 game/ 下」写的）；`game/.gitignore` 是 Cocos 编辑器维护的，动它下次会被改回来。

**`.meta` 必须 commit** —— 用编辑器打开 `game/` 后它会给 `assets/logic/` 下每个 `.ts`
生成 `.meta`，那里面存 uuid 映射。漏 commit 的话，场景里拖好的引用下次全变空。

#### ⬜ 合并的后半截：你那边换工作副本

服务器端合完了，但你 Windows 上那个 Cocos 工程目录还是旧的（独立仓库，指向 `master`）。
等 `main` push 上去之后：

```
① 现在那个工程目录改名备份，别删
② git clone git@github-luyouse-user:luyouse-luka/Crazy-Kitchen.git
③ 用 Cocos 编辑器打开 clone 出来的 game/ 子目录（不是仓库根）
④ 编辑器会给 assets/logic/ 下 20 个 .ts 生成 .meta → commit 掉
⑤ 确认能构建、能真机跑，再删 ① 的备份
```

⚠ **③ 打开的是 `game/`，不是仓库根** —— 打开仓库根会让编辑器在根目录再生成一套
`assets/` / `settings/`，等于又开一个新工程。

顺带一提，`game/package.json` 里工程名还是 `NewProject`，要不要改成 `kitchen-chaos` 你定
（`uuid` 才是编辑器认的标识，改 `name` 应该无害，但我没验证过，所以没动）。

#### 已废弃：切换到 git 工作流的顺序

原来这里有一份「先 push → 备份孤儿拷贝 → clone → 启用 ignore → 删备份」的五步顺序。
**2026-09-02 已全部执行完毕**，留个记号免得下轮以为还没做。
本地那两份 `.unison-bak` 如果还在，确认 clone 内容齐了就可以删。

#### §A7 · 分层落地验证（2026-09-02 备好探针，等你构建一次）

**验的是什么**：`game/assets/logic/` 那 20 个纯 TS 文件，Cocos 的编译链吃不吃得下、
打不打得进微信小游戏产物、跑出来的结果和我在服务器上跑的**是不是同一个数**。
前两条过不了就走 §8 风险表的退路（logic 改成 outFile 预编译成 js）。

**探针三件套**（我已写好并提交）：

| 文件 | 作用 |
|---|---|
| `game/assets/scripts/a7-check.ts` | 纯 TS，零 `cc` 依赖。把 8 个 logic 模块各调一遍，输出 9 行 |
| `game/assets/scripts/A7Probe.ts` | Cocos 组件，`start()` 里把那 9 行 `console.log` 出来 |
| `tools/a7-expect.ts` | 同一份 `a7-check.ts` 在 Node 里跑。`pnpm a7` |

关键在于**两边跑的是同一个文件**，所以判据不是「看着差不多」，是**逐字符相同**。

**你要做的**（前提：§A2-fix 末尾那五步换副本已完成）：

```
① 编辑器打开 game/ → 它会给 assets/logic/ 和 assets/scripts/ 下的 .ts 生成 .meta → commit
② 打开 test.scene，把 A7Probe 组件拖到任意一个节点上（挂哪个节点都行），保存场景
③ 构建 → 微信开发者工具 → 看 console
④ 把 console 里那 9 行原样贴给我
```

**期望输出**（`pnpm a7` 于 2026-09-02 实测，服务器 Node v24.19.0）：

```
A7_LOGIC_LINKED_KITCHEN_CHAOS
types ing=8 cook=raw/rare/medium/well/burnt mood=8 station=fridge/grill/assembly/serve/sink
recipe core=true cook=medium n=3
order ok=true cookOk=true missing=[] forbidden=[]
rng 7,70,90,97
vec2/collision dist=5.000 rot=4.000,-3.000 hit=true reach=true
difficulty lastDay=20 stars=3/6/8 got=3
sim arrived=18 served=9 wrong=0 timedOut=9 burnt=0 peak=4 rate=0.5000
A7_LOGIC_LINKED_KITCHEN_CHAOS DONE
```

**三层判据，从弱到强**：

| 层 | 判据 | 过不了说明什么 |
|---|---|---|
| 1 | 产物里 `grep -r A7_LOGIC_LINKED_KITCHEN_CHAOS` 有命中 | 组件根本没编译进包 —— 多半是 `.meta` 没生成或组件没挂上 |
| 2 | 产物里搜得到 `burnt` / `menacing` / `assembly` 这些 logic 独有的字面量 | 组件进了但 logic 被 tree-shake 掉了 |
| 3 | console 那 9 行与 `pnpm a7` **逐字符相同** | 数字对不上 = Cocos 的编译链把 logic 编译成了另一个语义（浮点、装饰器、target 差异），这是最该怕的一种失败 |

第 3 层是真正的价值所在：`sim` 那行跑的是完整一天的无头模拟（几千帧），
它对得上，等于把 `logic/` 全部 8 个模块在真机运行时环境里过了一遍。

**签收之后**：`A7Probe.ts` 从场景上摘掉即可，两个文件先留着 ——
M2 换 Cocos 版本或改构建配置时重跑一遍最省事。

⚠ **`pnpm a7` / `pnpm sim` 在服务器上曾经跑不起来**（`vite-node@6` 拉的是 `vite@8`，
`vitest.config.ts` 里那条 `esbuild.tsconfigRaw` 对它无效）。已改成自愈：
`tools/ensure-cocos-tsconfig.mjs` 在 `game/temp/tsconfig.cocos.json` 缺席时补一份最小版，
由 `presim` / `prea7` 自动调起。**编辑器那份永远优先**，脚本只在文件不存在时才写。

---

## ✅ 已定

- **引擎 = Cocos Creator 3.8**
- **视角 = 斜 45° 固定俯视「轻 3D」**（不再是第三人称可转 / 第一人称）
- **横屏**，设计分辨率 1280×720（`GDD.md` §1 的「竖屏」作废）
- **交互 = 左拇指摇杆 + 右拇指交互键**，双手同时输入（横屏带来的，见 M2）
- **局制 = 一天一关，营业 3–4 分钟，三星评分**（学胡闹厨房，见「玩法基准」节）
- **两款参考作各占时间轴一半**：营业中 = 胡闹厨房 / 营业前后 = Burgie's
- **美术参考**：两者共有的低模卡通厨房

视角这一改不只是美术选择，它**同时解掉了两个问题**：

1. **玩法上**——上一版我标记过「相机可转与『同时盯四个灶台』的混乱核心有真实冲突，转身就看不见烤炉」。
   固定俯视角一眼看全整个厨房，这个冲突消失了。**这才是选它的主要理由**，性能是顺带的。
2. **性能上**——省掉相机碰撞、遮挡半透、动态视锥剔除，光照可全烘焙，场景可整块合并。见第二节。

> 📌 **关于 Burgie's 的节奏冲突，上一版的判断已被修正。**
> 我先前说它 cozy/挂机的节奏与核心卖点相反、只能借美术——**不准确**。
> 正解是借在**正确的时间段**：`GDD.md` §12 的「营业前 / 营业中 / 打烊后」三段框架里，
> 营业中归胡闹厨房，前后两段归 Burgie's。详见下面的「玩法基准」节。

## ⏸ 还没定

**① 微信这一端做到什么程度。** 你已选「走完审核真正上线」，方案按此排了 M7 与软著时间线。
若降级成「开发者工具 + 真机预览，不提审」，可砍掉软著、类目申报、内容自查，
且 AI 顾客维度池不必提前剔除敏感值。**这条随时可以降级，不可升级**（软著要提前排）。

**② 游戏名 —— 不再是「不阻塞开发」，它有截止日期。**
软著证书上的软件名称必须与上架的小游戏名完全一致，而软著要等 6–12 周。
所以**游戏名最迟在 M2 结束时定死，之后改名 = 重新申请 = 推迟上线两三个月**。
现有 4 个候选（疯狂后厨 / AI后厨 / 神经病餐厅 / 厨神营业中）在那之前想清楚就行。

美术细节未定，确实不阻塞——几何体能一路撑到 M6。

---

## Context

`project/kitchen-chaos/` 现有三份文档：**本 ROADMAP（执行路线，正本）**、`GDD.md`（想法记录）、
`docs/ai-customer-v1.md`（顾客卡设计稿）。**仍是零代码**——第一行代码从 §10「第一周就做这些」的线 B 开始。
本方案把它变成一条可执行的路线：**斜 45° 轻 3D 厨房经营游戏 → 微信小游戏正式上架**。

四个前提在讨论中确定，与 GDD 原稿有出入的地方以此为准：

| 前提 | 说明 | 对 GDD 原稿的修正 |
|---|---|---|
| **轻 3D，斜 45° 固定** | 真 3D 渲染 + 2D 玩法空间，相机不跟随旋转 | 原稿默认 2D 俯视 → 现为 3D 渲染，但玩法维度未变 |
| **你自己动手，AI 辅助** | 你操编辑器/调手感/真机；AI 写代码、理逻辑、优化动画 | 原稿未涉及分工 |
| **真上线** | 走完软著 + 审核，不是跑通就算 | 原稿未涉及 |
| **不计成本、不谈盈利** | 学习与兴趣驱动 | **原稿第 9 节整套成本论证作废**（架构结论仍成立，但理由换成稳定性与平台约束） |

> ✅ **已于 2026-08-07 同步到 `GDD.md`**：附录「待定事项」的引擎与美术风格两行已结案
> （Cocos 3.8 / 低模卡通斜 45°）；§1 的屏幕方向已改为**横屏**；§9 成本论证已标作废、
> §10 阶段划分已指向本文件 M0–M7；并新增 §13 记录本方案带来的六条前提。

> 胖丁冲冲的 Cocos 教训**不适用于本项目**——那个项目在验证「AI 独自能做到什么程度」，
> 所以 GUI 编辑器是硬障碍。本项目你自己上手，编辑器可用，约束解除。

---

## 玩法基准

### 一、先立自己的，再谈借鉴

> ⚠ **这一节上一版的组织方式是错的**，按「胡闹厨房有哪些机制、我们抄几条」来排。
> 那个框架会让本项目慢慢变成「胡闹厨房的手机单人版」——独有的东西被挤到边缘。
> **正确顺序是先立柱子，再从参考作里取工具。**

本项目的三根柱子来自 `GDD.md` §1，**不是从参考作推出来的**：

| | 柱子 | 参考作里有吗 |
|---|---|---|
| **1** | **AI 顾客不断整活** | ❌ 两款都没有。**这是本项目唯一的原创主张** |
| **2** | **厨房不断失控** | ✅ 胡闹厨房的强项，手法可借 |
| **3** | **单人 · 碎片时间 · 微信** | ❌ 两款都是 PC 取向，一款还是多人 |

**三根里只有第二根能大量借鉴。第一、三根必须自己长出来——参考作在这两条上帮不上忙，甚至会误导。**

### 二、单人不是缺陷，是第一根柱子能成立的前提

上一版我写「AI 顾客顶替队友的功能位」——**这个说法把主角说成了替补，收回。**

AI 顾客不是因为没队友才拿来填坑的东西，它是这个游戏一开始的主张
（`GDD.md` §11：「这部分如果成立，整个项目就有继续做下去的价值」）。

而且因果是反的：**正因为是单人，AI 顾客才做得起来。**

| 单人给的设计自由 | 多人为什么做不到 |
|---|---|
| 顾客台词有 30 字的阅读时间 | 队友在催，没人看字 |
| 随时开局、随时放下 | 要约人、要同步在线 |
| 难度曲线可精确标定 | 人数变化直接破坏平衡（这正是 M1 模拟器成立的前提） |
| 长线记忆与养成成立 | 进度不同步，「第 20 天他带朋友来」讲不通 |

**胡闹厨房塞不下一句 30 字的离谱订单，因为两个人都在跑。单人可以。**
这是本项目的位置，不是它的短板。

### 三、裁决规则：冲突时砍参考作

> **参考作的手法与本项目三根柱子冲突时，砍参考作的，不砍柱子。**

这条规则不是表态，它会被真正用到。例：胡闹厨房靠「场景即敌人」（地震、断桥）制造混乱，
但那些设计是为**逼两个人重新协商**服务的——本项目没有那个协商，硬搬只剩恶意。
所以**砍掉它的形式，只留「制造压力」这个目的**，换手段实现（见第五条）。

### 四、可以从胡闹厨房拿的（工具箱，不是骨架）

约束：单人 · 触屏两个键 · 无物理引擎 · 一局 3–4 分钟。

| 胡闹厨房的机制 | 判断 | 理由 |
|---|---|---|
| **关卡制，一关 3–4 分钟** | ✅ **抄** | 完美契合小游戏碎片时间。与 GDD 的「天数推进」合并：**一天 = 一关** |
| **三星评分** | ✅ **抄** | 给重玩理由，且和 GDD §7 的奖励系统天然接上 |
| 食材加工链（取→烹→装盘→上菜） | ✅ **抄** | 长按即可，触屏没问题。V1 只有汉堡，链条短，够用 |
| 烧焦 → 起火 → 灭火器 | ✅ **抄** | GDD §3.2 已有 |
| 盘子有限 → 洗碗池 | ✅ **抄** | GDD §3.2 已有。这是**最好的机制**——它不加新按钮，只是让已有资源变紧 |
| 难度缓慢爬升维持心流 | ✅ **抄** | 正是 M1 无头模拟器要调的东西 |
| **场景即敌人**（地震、断桥、移动台面、老鼠） | ⚠️ **改造** | 见下方，单人下直接搬会变成纯恶意 |
| 关卡中途布局改变 | ⚠️ **改造** | 同上，降级成「混乱事件」 |
| **投掷食材给队友** | ❌ **砍** | 触屏要方向+力度，很难；且单人没有接的人 |
| 冲刺键 | ⚠️ **待定** | 要占右手第二个键。等 M2 定完「拿放/操作」再看还有没有位置 |
| 队友分工与沟通 | ❌ **无法移植** | 由 AI 顾客顶这个功能位（见上） |

### 五、「场景即敌人」的单人化改造原则

这是本节最需要想清楚的一条。

胡闹厨房的地震把两个玩家隔开之所以**有趣**，是因为它逼出一次**重新协商**：
「你守灶台我跑腿」。单人玩家遇到同一件事，只剩下「游戏在玩我」——纯挫败，没有决策。

> **改造原则：单人版的干扰必须「可决策」，不能是「纯剥夺」。**
> 玩家要能做取舍，压力才成立；不能做取舍的只是恶意。

| ❌ 纯剥夺（别做） | ✅ 可决策（这样做） |
|---|---|
| 地板突然消失，你走不过去 | 送货到门口，**你选**什么时候去签收（去了就丢一单，不去货会积压） |
| 随机没收手上的食材 | 停电 30 秒，灶台停摆——**可预期**，你能提前把该烤的烤上 |
| 顾客无理由暴走 | 卫生检查要求先擦净某台面，**你选**先擦还是先出餐 |

判据很简单：**这个干扰发生时，玩家有没有一个「先做 A 还是先做 B」的选择？** 没有就重新设计。

### 六、Burgie's 值得学的两件事（都不是美术）

它和胡闹厨房不冲突，因为**两者各占时间轴的一半**。`GDD.md` §12 的三段框架正好接上：

```
营业前   备料、看今天预告谁会来          ← Burgie's 这一侧
营业中   混乱核心，一秒注意力都不剩       ← 胡闹厨房那一侧
打烊后   结算、小费、升级、员工闲聊       ← Burgie's 这一侧
```

紧张之后的松弛比全程温馨更强。**互不冲突，还互相衬托。**

**① 经济闭环：顾客评价 → 小费 → 厨房升级。**
它把「服务质量」直接接到「成长」上，不需要额外的货币系统。
本项目的 GDD §6（AI 评价）和 §7（奖励）本来是两个系统，按这个思路应该合成一条链。

**② 「简单的机制，非常精致，彼此之间紧密相连」——这句评价是它真正的设计成就。**

这对本项目是个警告。`GDD.md` §12 已经攒了一堆发散想法（外卖、招聘员工、员工交流、爱心线），
每个单看都不错。定一条纪律挡住它们：

> **新机制必须与已有机制产生耦合，否则不加。**
>
> - ✅ 「盘子有限 → 洗碗」耦合：它不加新按钮，只让已有资源变紧
> - ✅ 「外卖骑手」耦合：与堂食抢同一个厨房，是免费的压力源（GDD §12.1 的判断是对的）
> - ⚠️ 「招聘员工」**不耦合**：它是并列的新系统，还会削弱核心
>   （GDD §12.2 自己已经识别到这个风险，结论正确——员工要么不做菜、要么会犯错）

**不学的**：Burgie's 有物理系统，本项目明确不引物理引擎（§1 已论证，省 0.7–1.2MB 主包）。
它的挂机 / 番茄钟 / 直播三个模式也不学——那是给「桌面陪伴」这个定位服务的，与本项目无关。

### 七、这一节对计划的影响

| 影响 | 落到哪 |
|---|---|
| 「一天 = 一关 + 三星评分」成为结构基准 | M1 的难度曲线按「一局 3–4 分钟」标定，不是无限续摊 |
| 混乱事件按「可决策」原则筛 | M4 每加一个混乱源，先过上面那条判据 |
| **打烊后那一段目前无人认领** | ⚠️ 见下 |

⚠ **发现一个缺口**：现在的里程碑里，「营业中」被 M3/M4 覆盖得很扎实，
但**「打烊后」的结算 → 小费 → 升级这条经营循环没有任何里程碑负责**。
M2 只有「金币结算」，M4 只有「天数推进」——**天数推进如果后面接不上升级，那一天结束就是空的**。

建议并入 M4（它本来就要做天数推进），任务清单已相应补齐。

---

## 一、技术选型

**引擎：Cocos Creator 3.8**

| | 理由 |
|---|---|
| 语言 | TypeScript，与你现有技能同源；组件即 .ts 文件，AI 可全程参与 |
| 上线 | 构建直出 wxgame，主包可压进 4MB。**不需要 CDN、不需要备案域名** |
| 3D | 本项目的 3D 需求（固定/跟随相机、低模、简单动画）远在 Cocos 能力之内 |
| 社区 | 中文文档，小游戏相关的坑能搜到中文答案 |

**否决 Unity 的原因**（微信官方 `minigame-unity-webgl-transform` 实测数据）：

```
微信硬限制        主包 ≤4MB   分包 ≤4MB/个   总计 ≤16MB
Unity WASM 实测   首包 3-5MB  子包 7-15MB    wasmcode 10-25MB
```

资源基本必须上 CDN → CDN 需备案域名 → 上线清单从「软著 + 审核」膨胀成「软著 + 审核 + 备案 + CDN + WASM 分包调优」。
这些工作与你想学的东西无关。

**选型失效条件**（写在这里，将来回看不用重推）：
- 若真实目标是「行业标准技能」→ Unity/C# 简历价值高一档，值得付那些税
- 若后期要做复杂角色动画（IK、布娃娃、动作融合）→ Cocos 3D 生态会不够用

**退路成本**：见第三节的分层架构。纯逻辑层零引擎依赖，换引擎可原样搬走。

### 「轻 3D」的精确落点

这个词必须定义死，否则 M2 之后会失控——每加一个「顺手也做了吧」的 3D 特性，都在往回走。

| 维度 | 定 |
|---|---|
| 渲染 | **真 3D**：正交或小 FOV 透视相机、深度排序、贴地阴影、烘焙光照 |
| 玩法空间 | **2D**：角色在 XZ 平面移动，Y 恒定。无跳跃、无斜坡、无楼层 |
| 碰撞 | **手写圆形 / AABB**，不引入物理引擎 |
| 顾客寻路 | 路点（waypoint）或格子，不做 NavMesh |
| 相机 | 固定角度、固定或轻微跟随位移，**不可旋转、不可缩放** |

**「不引入物理引擎」是笔实在的账**：Cocos 的 ammo 物理模块 wasm 约 1.2MB / js 约 0.7MB，
对 4MB 主包是一大块。而这个游戏需要的全部碰撞就是「角色别穿过灶台」和「角色进入工位触发范围」——
二十行圆形距离判断解决，还能整个写进零引擎依赖的 `logic/` 层，Node 里就能跑测试。

**玩法空间是 2D 这一条同时是架构红利**：`logic/` 里的坐标一律用 `{x, z}`，
不出现 Cocos 的 `Vec3`，引擎无关性比上一版更彻底。

---

## 二、性能：斜 45° 轻 3D 在微信上的账

**结论先行：这套组合在微信上性能是宽裕的，渲染不是瓶颈。会真正卡住你的是包体、iOS 内存、JS 逻辑帧——
三个都不在「画面好不好看」这条线上。**

### 2.1 基线数字（官方文档 + 社区实践清单）

| 指标 | 阈值 |
|---|---|
| 主包 | ≤4MB（红线）/ ≤3.8MB（安全线） |
| 整包含分包 | ≤16MB（红线）/ ≤15MB（安全线） |
| DrawCall | 首屏 ≤100 / 游戏内 ≤200 / 峰值 ≤350 |
| FPS | 高端 55–60（最低 48）/ 低端 28–32（最低 24） |
| 内存峰值 | 低端机 ≤700MB / 高端机 ≤1000MB |
| 首屏加载 | ≤3000ms（理想 ≤1500ms，4500ms 起流失率明显上升） |

### 2.2 DrawCall：十倍余量，不用担心

一个 8m×6m 厨房、6 个工位、同屏 6–8 个角色的粗算：

| | 未优化 | 优化后 | 手段 |
|---|---|---|---|
| 静态场景（地板/墙/固定家具/装饰） | 30–50 | **2–4** | 共用一张图集 + 合并成单 mesh。**相机不动，永远不需要单独剔除某件家具** |
| 角色（玩家/顾客/骑手） | 6–8 | **1–2** | GPU Instancing（同模型换色） |
| 角色阴影 | 6–8 | **1** | 贴地椭圆贴片（Blob Shadow）随角色一起 instancing |
| 手持物 / 台面上的食材 | 10–15 | **2–3** | 同一图集 |
| 粒子（火/烟/水花） | 4 | **2–3** | 共用材质 |
| UI（HUD/订单卡/气泡/摇杆） | 30–40 | **3–5** | 合图；字体单独一批 |
| **合计** | **约 90–125** | **约 12–20** | |

即使一行不优化也在「游戏内 ≤200」之内；优化后离红线有十倍余量。
**别为 draw call 做提前优化，把预算花在下面三条上。**

### 2.3 斜 45° 固定相机白拿的东西

相对上一版的第三人称可转 / 第一人称，这些工作量与开销直接消失：

- 相机碰撞检测（撞墙、穿模）
- 角色被前景遮挡时的半透明/描边 shader
- 动态视锥剔除与 LOD —— 场景可整块烘成一个 mesh
- 双指旋转/缩放手势 —— 摇杆独占触摸，交互简单一大截
- **实时光照** —— 相机不动 + 场景静态 ⇒ lightmap 烘一次用到底，运行时零光照计算
- 模型背面 —— 家具背面、墙外侧永远看不见，可直接删面

### 2.4 真瓶颈一：包体

3D 引擎是死成本，砍不动，其余全要挤。~~参考数字：Cocos 2D 项目优化后 `cocos-js` 约 1.64–1.68MB、
主包 1.94–2.14MB；**3D 管线更重，`cocos-js` 预算按 2.2–2.8MB 估**~~ —— **A5 已实测，估高了 2.5 倍以上**，
真数见下方 §2.4a，本节表格的 `cocos-js` 行已按实测改写。

| 项 | 预算 | 备注 |
|---|---|---|
| `cocos-js`（3D 裁剪后） | **0.86MB（实测）** | 死成本。3.8.8 起 19 个模块走微信**引擎插件**不计包体，只剩 6 个本地。仍可再裁 `physics-2d-box2d` 365KB |
| 中文字体子集 | 0.3–0.8MB | ⚠ **整套中文字体 >10MB**，必须 fontmin 提字（减 80–90%） |
| 3D 模型 | 0.5–1MB | 低模无贴图，20 物件 + 4 角色 |
| 贴图 | <0.1MB | 卡通风格只需一张 512 调色板图集 |
| UI 图集 | 0.3–0.5MB | ≤1024×1024 |
| 音效 | 0.5–1MB | WAV→OGG 减 65–70% |
| 顾客卡 JSON | 10000 张 ≈ 2MB | **装不进主包** |

**所以分包策略必须在 M0 就定，不能拖到 M6 的适配阶段。** 好消息是顾客卡是最理想的分包对象——
纯数据、非首屏必需，且 GDD 本来就有「天数推进」机制：**首包只带 300–500 张够玩前几天，其余按天解锁下载**。

### 2.4a 空项目包体实测（A5 · 2026-09-02 第二次构建，取代第一次）

来源：`project/wechatgame/`（Cocos 3.8.8 空模板 + 一个 `test.scene`，真机已跑通）。
判据脚本 `tools/pkgsize.ts`，跑法 `pnpm size`（默认就指这个目录），孤儿 0 个。
产物 09-01 **09:01** 构建，工程配置最后一次改动在此之前 —— 这组数字**不再有时效问题**。

| 项 | 09-01 07:35 构建 | 09-01 09:01 构建 | 变化 |
|---|---|---|---|
| **主包有效总量** | 2299.8 KB | **2428.0 KB** | +128 KB ✅ 红线 4096 / 安全线 3891 |
| `cocos-js` | 862.5 KB（19 模块走插件） | **1844.3 KB（单体，0 插件）** | **+982 KB** |
| `assets` | 1249.1 KB | **397.2 KB** | **−852 KB** |
| `src` | 30.0 KB | 27.0 KB | — |
| 外壳 / 适配 | 158.1 KB | 159.5 KB | — |

两笔近千 KB 的变化方向相反，净额只有 128 KB，**别被总量的平稳骗过去**。

#### ✅ 解决了：850 KB 天空盒自己没了

上一版记为「⏸ 不知道怎么去」的那两个 cubemap（`d032ac98…` 575.5 KB +
`6f01cf7f…` 274.5 KB），**重新构建后不在包里了** —— `find assets -name "*d032ac98*"` 零命中。
上一版的推断「它们是被依赖收集进来的，不是场景直接引用」得到印证：
依赖关系一变（关模块 / 重新收集），它们就不再被收进来。
`assets` 剩下的 397 KB 里 356 KB 是 `internal/import/06/06ba65fee.json`（内置 effect 合并 json，
着色器，**死成本**），逻辑资源仍然只有几 KB。

#### ✅ 解决了：2D 物理真的裁掉了

`engine.json` 里关的 `physics` / `physics-2d` 这次生效了。判据是在单体 `cc.js` 里搜特征符号：
`b2World` `b2Body` `box2d` `PhysicsSystem2D` `RigidBody2D` `physics-2d` 全部 **0 命中**，
3D 的 `PhysicsSystem` `RigidBody` `ammo` `cannon` 也全 0。上一版预估的 406 KB 省下来了。

#### ⏸ 待你确认：微信引擎插件这次没开

上一次构建 `cc.js` 是 7.4 KB 的入口文件，引用 25 个模块、其中 19 个带 `plugin:cocos/` 前缀
（走微信引擎插件 `wx0446ba2621dda60a`，**不计 4MB 主包**）。
这一次 `cc.js` 是 **1.8 MB 的单体文件，`grep -c "plugin:cocos" = 0`** —— 引擎整个进了主包。

**代价换算**：引擎 +982 KB，而中间还裁掉了 406 KB 物理，所以「不走插件」的真实成本约
**1388 KB**。开回插件的话主包大约能回到 **1040 KB**，等于把红线余量从 1668 KB 拉到 3056 KB。

| | 走引擎插件 | 不走（当前） |
|---|---|---|
| 主包 | ≈1040 KB | 2428 KB |
| 首次进入 | 要下载引擎插件，**吃首屏时间** | 无额外下载 |
| 可控性 | 插件版本跟微信走 | 引擎版本自己钉死 |

**这是个决策，不是 bug，我不替你定。** 请你在 Cocos 构建面板里核一下
「使用微信小游戏引擎插件」这一项这次是不是没勾 —— 如果是有意关的，把理由写进这一节；
如果是手滑，下次构建勾回来再量一次。M6 真机测首屏必须冷启动测，那时候这笔账才算得准。

#### 结论（按当前这次构建）

- **引擎不便宜了**：上一版「3.8.8 引擎只占 862 KB」的结论**成立的前提是走引擎插件**。
  不走插件时引擎 1844 KB，占主包 76%。两个数字都对，别混着引用。
- **包体大头从资源转回引擎**：`assets` 397 KB 里 356 KB 是着色器死成本，
  真正属于「我们的内容」的还是零。
- 红线 4096 KB 下**留给内容约 1668 KB**（走插件则约 3056 KB）。
  顾客卡 10000 张 ≈ 2MB，**当前配置下装不进主包，分包策略仍然要做**。

### 2.5 真瓶颈二：iOS 内存（最可能真机闪退的地方）

- **iOS 上小游戏与微信共进程**：2G RAM 机型（iPhone 6s/7/8）上限 1G，3G RAM 机型上限 1.4G，超了被系统杀。
- **Android 是独立进程**，内存问题小得多。所以真机测试**必须包含一台旧 iPhone**，只测安卓会漏掉这类崩溃。
- 单个双声道音频实例约占 20MB —— 音效一律单声道。
- 纹理用压缩格式而非 PNG（约省 50%）。

⚠ 这一整类问题**本机 headless 一条都测不出**（无 GPU、无真实内存压力），只能真机。

### 2.6 真瓶颈三：JS 逻辑帧（和架构直接挂钩）

Cocos 3.8 的渲染走 native/WASM，但**你写的所有游戏逻辑跑在 JS 里**。每帧要遍历顾客耐心、烤炉火候、
玩家与工位距离、订单匹配。如果热路径上每帧 `new` 一堆临时对象（典型：算距离顺手 `new Vec3()`），
会触发频繁 GC → 帧时间尖刺 → 手感「一卡一卡」。**这在 draw call 完全健康时也会发生，而且更难查。**

于是给 `logic/` 加第二条铁律：

> **热路径零分配。** `update()` 路径上不创建对象/数组/闭包，用固定数组 + 对象池。
> 判据：Node 里跑 10000 帧模拟，`process.memoryUsage().heapUsed` 增长应趋近平坦——
> **这条不用开编辑器、不用真机就能验证**，正是分层架构换来的。

### 2.7 iOS 高性能模式（免费，别忘了开）

微信为 iOS 提供了新渲染后端，**免费、无 DAU 门槛、无主体限制**：
公众平台 → 能力地图 → 研发能力 → 生产提效包 → 开通，然后 `game.json` 加 `iOSHighPerformance: true`。

官方压力测试（12000 条鱼）：iPhone 11 Pro Max 从 **10–15 帧 → 48–50 帧**。要求 iOS 14+、微信 8.0.18+。

代价要提前知道，因为**它会反过来约束美术管线**：

- **纹理压缩只能用 ASTC / ETC1，不支持 ETC / PVRTC** ← 这条决定你导出贴图用什么格式，越早定越好
- 不支持 GameServerManager / RecorderManager / VideoDecoder（本项目都用不到）
- iOS 15.0–15.3 上 WebGL2 不可靠
- 强制严格模式，会暴露原先藏着的 JS 错误
- 低端机内存上限更严（2G 机型 1G、3G+ 机型 1.5G）

### 2.8 性能这一节的验证态度

上面除了官方阈值和官方压测数字，**其余都是预算不是实测**。
本机 headless 无 GPU，跑不出任何有意义的帧率或内存结论（见 memory `headless-chromium-probe-limits`）。
所以 M0 的第一件事就是把空 3D 项目推到真机看基线包体与帧率，**用真数替换掉 2.4 的估值**。

---

## 三、架构：引擎无关的逻辑层

**这是整个方案的支点。** 它同时解决三件事：换引擎的风险、AI 能帮多少、逻辑怎么测。

### 3.1 ⚠ 目录结构的硬约束（上一版这里是错的）

**Cocos Creator 只编译 `assets/` 目录下的脚本。** `assets/` 之外的 TS 要参与运行，
必须靠 tsconfig 的 `outFile` 编译成 js 再吐回 `assets/` —— 那就多出一个编译步骤、
一份产物、一套 source map，调试时你看到的报错行号还对不上源码。

上一版把 `logic/` 和 `game/` 放成平级目录，**这在 M0 就会撞墙**。

**修正：`logic/` 直接住进 `game/assets/logic/`。** 它仍然是纯 TS、零 Cocos 依赖，
只是物理位置在 assets 里，于是：Cocos 照常编译，vitest 也能直接跑同一批 `.ts` 源文件，
**不需要任何中间编译步骤**。铁律一条不变。

```
project/kitchen-chaos/
├─ game/                          ← Cocos 工程根（本地 Windows 用编辑器打开的就是这个）
│  ├─ assets/
│  │  ├─ logic/                   ★ 纯 TypeScript，零 Cocos 依赖，Node 直接可跑
│  │  │  ├─ types.ts              全部数据结构（食材、订单、顾客卡、事件）
│  │  │  ├─ vec2.ts               { x, z } 平面坐标与距离，不用 Cocos 的 Vec3
│  │  │  ├─ collision.ts          圆形/AABB 碰撞与工位触发范围（替代物理引擎）
│  │  │  ├─ recipe.ts             食材 → 汉堡的组装规则
│  │  │  ├─ order.ts              订单判定：手上这个汉堡满不满足顾客要求
│  │  │  ├─ customer.ts           顾客状态机：入店/点单/等待/进食/离开
│  │  │  ├─ chaos.ts              混乱事件调度器
│  │  │  ├─ economy.ts            金币、升级、解锁
│  │  │  ├─ sim.ts                ★ 无头模拟器：不开引擎跑完整一局（见 M1）
│  │  │  └─ API.md                ★ 公开接口清单，我改接口必同步改它（见第六节）
│  │  ├─ scripts/                 Cocos 组件：只做渲染 + 输入 + 调 logic
│  │  ├─ scenes/                  场景（你在编辑器摆）
│  │  ├─ prefabs/                 几何体角色、工位
│  │  └─ resources/               customers.json 等运行时数据
│  ├─ settings/ · package.json    Cocos 工程配置
│  └─ tsconfig.json
│
├─ pipeline/                      ← AI 内容生产管线（离线 Node，与引擎完全无关）
│  ├─ dimensions.json             维度池
│  ├─ customer.schema.json        顾客卡 Schema
│  ├─ gen.ts · validate.ts        批量生成 / 校验
│  └─ out/                        生成结果，验收通过才拷进 resources/
│
├─ tests/                         ★ vitest 单元测试 —— 必须在 assets/ 之外，见下
│  ├─ *.test.ts                   import '../game/assets/logic/<模块>'
│  └─ fixtures/customers.json     21 张手写顾客卡
├─ vitest.config.ts               ← 指向 tests/**/*.test.ts
├─ package.json                   ← 仅供测试与管线用，不进 Cocos 构建
├─ GDD.md · ROADMAP.md · docs/
```

> `package.json` / `vitest.config.ts` 放在**仓库根**而不是 `game/` 里，
> 避免和 Cocos 自己的 `game/package.json` 打架。

#### ⚠ 测试必须在 `assets/` 之外（2026-09-02 用一次构建失败换来的）

上一版把 `__tests__/` 放在 `game/assets/logic/` 里面，**这会让构建直接失败**：

```
build task failed! Error: execute-task buildScriptCommand failed with code -1
```

**Cocos 编译 `assets/` 下的每一个 `.ts`，不区分它是不是测试。** 测试文件 import 的三样
东西它全解析不了 —— `vitest`（devDependency，构建时不存在）· `ajv/dist/2020`（同上）·
`'../../../../pipeline/customer.schema.json'`（**指向 `assets/` 之外，Cocos 不允许**）。

所以规则是：**`assets/` 下只放「要跟着进包」的东西**。判据一条命令：

```bash
find game/assets -name '*.ts' -exec grep -h "from '" {} + | sed "s/.*from //" | sort -u
# 只准出现相对路径和 'cc'。冒出任何裸包名或 ../../ 出 assets 的路径 = 下次构建会炸
# （别写成 game/assets/**/*.ts —— bash 默认不开 globstar，那样只扫得到一层）
```

`logic/API.md` 留在 `assets/logic/` 里是**有意的**（接缝文档贴着代码才记得改）。
Cocos 会给它生成 `.meta` 当未知资源导入，不引用就不进包 —— **这不是 bug，别去修**。

### 3.2 三条铁律，都能不开编辑器验证

| 铁律 | 判据 | 什么时候跑 |
|---|---|---|
| ① `logic/` 零引擎依赖 | `grep -rn "from 'cc'" game/assets/logic/` = 0 | 每次提交前 |
| ② 热路径零分配（见 §2.6） | Node 跑 10000 帧 `sim.ts`，`heapUsed` 增长趋近平坦 | 每个里程碑收尾 |
| ③ `scripts/` 不写玩法规则 | Cocos 组件里不出现数值判定，只调 `logic/` 的公开 API | 代码走查 |

铁律③是新加的，防的是最常见的腐化方式：**「这个判断很小，就写在组件里吧」**——
半年后规则散落在 20 个组件里，既测不了也搬不走，分层就白做了。

带来的直接收益：
- 你在编辑器调手感的同时，我在服务器改判定逻辑并跑测试验证，冲突面最小
- 「订单判定对不对」「逻辑帧有没有在漏内存」「难度参数够不够狠」都不用开游戏
- 换引擎时这部分零改动
- **碰撞与寻路也在这一层**（因为不用物理引擎），「角色卡在灶台里」这类 bug 也能写成单测
- **`sim.ts` 让整局游戏可以无头跑** —— 这是下面 M1 那个纸面原型阶段的基础

---

## 四、工程基建：先脱离 Unison

⚠ **这一步必须在写任何代码之前做，否则会真丢东西。**

现状：Unison 单向同步 `/home/ly/` → 本地，`prefer=newer + batch` 按 mtime 静默覆盖。
一旦变成「你在本地 Windows 的 Cocos 编辑器改场景、我在服务器改 .ts」的双向工作流，
你摆一下午的场景会被一次同步抹掉（EuroCave 第 32/35 轮已实测过这个坑）。

做法：

1. 定位 Unison 配置（`~/.unison/*.prf`），加 `ignore = Path project/kitchen-chaos`
2. 服务器 `git init`，建 GitHub private repo（复用 pangding-chong 的 SSH alias 方案）
3. 本地 `git clone` 到 Windows，Cocos 打开的是这个 clone
4. `.gitignore` 必须包含 Cocos 的 `library/` `temp/` `build/` `profiles/` `native/`
   —— 这几个是本地重建产物，进仓库会天天冲突
5. 约定：**你改 `game/assets/scenes|prefabs`，我改 `logic/` 和 `game/assets/scripts`**，
   各自 commit 各自 pull

---

## 五、里程碑

### 5.0 排期的三条原则（上一版违反了全部三条）

上一版是一条线性任务流。重排后依据这三条：

**① 三条线并行，不是一条串行。** 这个项目有三条互不阻塞的工作线：

| 线 | 谁 | 需要什么 | 阻塞关系 |
|---|---|---|---|
| **A 引擎链路** | 你 | Windows、Cocos 编辑器、真机、微信后台 | 我完全帮不上，也不需要等我 |
| **B 逻辑层** | 我 | 服务器 + Node，不需要引擎 | 不需要等你 |
| **C 内容验证** | 我 | 只要 API，连 Node 工程都不需要 | 不需要等任何人 |

上一版把 C 排到第 13 周（M4）。**这是最大的排期错误**——见原则②。

**② 最贵的假设最先验。** `GDD.md` §11 白纸黑字写着：

> 当前最重要的问题不是「Unity 还是 Phaser」，而是：AI 顾客到底能产生哪些
> 有趣、离谱、值得玩家截图分享的故事？**这部分如果成立，整个项目就有继续做下去的价值。**

这个判断是对的，而且验证它只要 **200 张卡、约 $2.5、一天**。上一版却把它排在第 13 周——
等于花 3 个月建好厨房，再回头问「这游戏的灵魂到底在不在」。**改到第 1 周，作为 go/no-go 闸门。**

同理，第二贵的假设是「厨房混乱真的能让人手忙脚乱吗」。上一版把它排在 M3（第 8–10 周）。
改到 M1 用无头模拟先验，**第 3 周就能知道答案**。

**③ 每个里程碑都要产出「能玩」或「能读」的东西。** 上一版 M1 的验收是「能走能拿能放」——
这个状态没有目标、没有反馈、没有失败，你从它身上学不到任何关于「好不好玩」的信息。
改成垂直切片：**哪怕丑到极点，也要能从头玩到尾**。

### 5.1 排期总览

时间按**业余每周 8–10 小时**估。理想值约 18 周；**算上生病、加班、没状态，现实是 6–8 个月**，
这个数字写在这里不是泄气，是让你在第 12 周还没上线时不会觉得自己失败了。

| | 里程碑 | 周 | 主线 | 出口 |
|---|---|---|---|---|
| **M0** | 三线并行开局 | 1–2 | A+B+C | ⛔ **go/no-go 闸门** |
| **M1** | 无头压力原型 | 1 | B（纯 Node） | 难度参数有解 |
| **M2** | 垂直切片：能玩完一局 | 2–3 | A+B | ⛔ **游戏名定死 → 提软著** |
| **M3** | 压力落地 | 2–3 | A+B | 核心卖点成立 |
| **M4** | 混乱全套 + 打烊后循环 | 3–4 | A+B | 手忙脚乱，且想再来一天 |
| **M5** | 内容扩产 | 1–2 | C | 10000 张卡进包 |
| **M6** | 适配 · 性能 · 真机 | 2–3 | A | 三项性能达标 |
| **M7** | 上架 | — | — | 等软著 + 审核 |

---

### M0 · 三线并行开局（1.5–2 周）

**三条线同时开跑，互不等待。**

#### 线 A · 引擎链路（你）

**先跑通空项目的完整发布链路，再写一行游戏代码。** 这条链路卡住，后面全白搭。

- [ ] Windows 装 Cocos Creator 3.8
- [ ] 第四节的 Git 迁移（**这一步在装编辑器之前做完，否则第一次同步就可能吞掉工程**）
- [ ] Cocos 新建 3D 空项目，**建时就设横屏 + 设计分辨率 1280×720**
- [ ] 构建微信小游戏 → 微信开发者工具能跑 → **真机扫码能跑**
- [ ] 注册小游戏 AppID（个人主体，免费）
- [ ] 开通 iOS 高性能模式（§2.7），据此定死纹理格式为 **ASTC**
- [ ] ⚠ **验证分层能落地**：在 `assets/logic/` 放一个纯 TS 文件，
      从 `assets/scripts/` 的组件里 import 它 → 编辑器不报错 → **构建产物里确实包含它**。
      这条不过，第三节的整个架构要改方案（见 §3.1）
- [ ] **记下 `cocos-js` 与主包各多大**，用真数替换 §2.4 的 2.2–2.8MB 估值

#### 线 B · 逻辑层骨架（我）

- [ ] 仓库根 `package.json` + `vitest.config.ts`，指向 `game/assets/logic/`
- [ ] `types.ts`（食材/订单/顾客卡/事件的全部数据结构）
- [ ] `vec2.ts` + `collision.ts` + 单测
- [ ] `recipe.ts` + `order.ts` + 单测：**把「一个汉堡怎么算做对了」写死并测通**
- [ ] 手写 20 张顾客卡进 `tests/fixtures/`，让后面几个里程碑不必等内容管线
- [ ] `API.md` 首版（见第六节的接口契约）

#### 线 C · AI 顾客验证（我）—— ⛔ 这是 go/no-go 闸门

- [ ] 重建 `pipeline/`（之前的脚本已删）：`dimensions.json` + `customer.schema.json`
- [ ] 维度池**从一开始就剔除敏感值**（宗教/暴力/擦边）——要过审，这是硬约束不是风险
- [ ] Opus 5 + Batch 生成 **200 张**，约 $2.5
- [ ] 按 `docs/ai-customer-v1.md` §7 三条判据打分：
      **截图率 ≥15% · 重复感衰减点 ≥50 张 · 机制耦合率 ≥40%**

> **⛔ 闸门：三条判据不过，就停下来重新设计顾客系统，不要开始建厨房。**
>
> `GDD.md` §11 的原话是「这部分如果成立，整个项目就有继续做下去的价值」。
> 这批卡纯文本读一遍就能判断——**如果卡片本身不好笑，套上厨房和动画也不会变好笑，只会更贵。**
> 花一天验证，省下可能三个月的方向错误。这是整个计划里投入产出比最高的一步。

**M0 验收**：真机上跑得起 Cocos 空场景 · 你知道还剩多少 MB · `npx vitest run` 全绿 ·
**200 张卡读完你自己想继续做**。

---

### M1 · 无头压力原型（1 周，纯 Node，不开 Cocos）

**这一整个阶段不碰引擎、不碰美术，只跑文本。** 上一版没有这个阶段，代价是「好不好玩」
要等到第 8–10 周才知道。

#### 为什么值得单开一周

核心卖点「手忙脚乱」的本质是一道**时间预算题**：

```
顾客耐心 (s)  vs  完成一单所需操作时间 (s) × 同时在场订单数
```

它跟画面、模型、动画**一点关系都没有**，是纯数值问题。而调参的成本差着两个量级：

| | 在 Cocos 里调 | 在 Node 里调 |
|---|---|---|
| 改一个数到看到结果 | 改码 → 编译 → 启动 → 手玩一局 ≈ 2 分钟 | 几毫秒 |
| 一轮能试多少组合 | 一晚上十几组 | 一秒钟上千局 |
| 能不能算出「理想玩家的上限」 | 不能，你的手速是变量 | 能，这是模拟器的强项 |

#### 具体做什么

- [x] ✅ `logic/sim.ts`：无头跑完整一局。输入=参数表，输出=文本时间线 + 统计
- [x] ✅ 贪心「理想玩家」：永远去处理最紧急的那一单，走位零失误、按键零延迟、**不齐不上菜**
- [x] ✅ 局长固定 210s，所有参数在这个时长内标定
- [x] ✅ 扫参数空间：`pnpm sim sweep interval|patience|spread|slots|extras|speed|interact`
- [x] ✅ 定三星评分线（`difficulty.ts` 的 `starsFor`）
- [x] ✅ 难度曲线表 `logic/difficulty.ts` —— M3/M4 直接调 `difficultyForDay(day)`

**M1 验收：两条都过。** `pnpm sim curve` 一条命令出曲线；`pnpm sim crash` 回答崩点。

#### 实测结论（2026-08-07，每点 32 局取平均）

**⛔ 崩点：同时 3 单。** 理想玩家在同时 1 单时完成 93%（还空闲 14 秒）、2 单 79%、
**3 单跌到 52%**、4 单 29%。真人会更早崩。

这个数字精确印证了本节开头那道时间预算题：基准布局下做一单约 15 秒，
15 × 3 = 45 秒 = 顾客耐心，临界点正好落在这里。**公式是对的，不是拍脑袋。**

**旋钮效力排序**（从基准 53% 出发的完成率跨度）：

| 旋钮 | 跨度 | 结论 |
|---|---|---|
| 工位间距 0.5→2 | 89% → 6% | 最强。**适合做 M4 的升级项**（花钱买「工位更近」），不适合做难度曲线 |
| 客流间隔 25→6s | 89% → 16% | 主旋钮，手感最平滑，曲线用它 |
| 额外配料数 0→4 | 75% → 9% | 极陡，加一样掉 20pt+。只能在最后几天慢慢加 |
| 单次操作耗时 0.2→1s | 72% → 7% | 手感参数，不该拿来调难度 |
| **顾客耐心 90→25s** | **63% → 40%** | ⚠ **最弱，只有 23pt** |

**三条反直觉的发现，直接影响 M2/M3/M4 的设计：**

1. **顾客耐心几乎调不动难度。** 砍 3.6 倍只掉 23pt —— 瓶颈是玩家的**操作吞吐量**，
   不是顾客肯等多久。客流 12 秒来一个、做一单要 15 秒，队伍必然堆积，给再多耐心也做不完。
   **想调难度别先动耐心。**
2. **多加烤炉是无效升级。** 基准火候（3/6/9/13s）下烤一块肉比玩家跑一趟还快，
   烤炉根本不是瓶颈 —— **跑腿才是**。1 个槽位和 4 个槽位完成率几乎一样（多槽还略差，
   因为要多盯一个计时器）。⚠ **M4「升级项·设备」如果做成「多一个烤炉」，玩家会感觉不到。
   应该做成「烤得更快」或「工位更近」。**（把火候窗口 ×5 后多槽立刻显效，
   证明这是参数取值问题不是调度写坏了）
3. **`bannedChance` 只压真人、不压理想玩家。** 理想玩家不会放错，所以这一维怎么加
   都不影响难度曲线；但它是真人的主要出错源。**这是唯一一个能单向增加真人压力的旋钮。**

**难度曲线**（`difficulty.ts`）：第 1→20 天，理想玩家完成率全程落在目标带内
（教学期 1–3 天 ≥92%，之后 78%–98%）。曲线只动客流间隔、耐心、同时上限、配料数、禁料概率，
**不动工位间距**（那是升级项）。

> 关于「理想玩家」的保守性：它不预判（肉快好了不会提前往烤炉走）、不做全局最优调度，
> 所以给出的是难度上限的**下界** —— 真实上限只会更高。M3 拿真人数据回来后，
> 偏差大说明该改的是操作和 UI，不是难度数值。

> 这不是过度设计，是把《PlateUp!》《胡闹厨房》这类游戏的数值策划工作前置。
> 顺带：`sim.ts` 后面还会被复用两次——铁律②的 10000 帧内存测试跑的就是它，
> M3 的「模拟器预测 vs 真人实测」对照也靠它。

---

### M2 · 垂直切片：30 秒能玩完的最丑版本（2–3 周）

**目标不是「能走能拿能放」，是「能从头玩到尾」。** 哪怕全是几何体、没有音效、UI 是白底黑字，
也必须有开始、有目标、有成功失败、有结算。

#### 你做

- [ ] 斜 45° 相机装配：正交投影起步，角度与高度做成 Inspector 可调，真机上调到「一眼看全厨房」
- [ ] 几何体厨房：地板 + 两面墙 + 冰箱 / 烤炉 / 组装台 / 出餐口四个工位（方块即可）
- [ ] 几何体角色：球 + 胶囊 + 方块，走路靠代码控制手脚旋转，零美术
- [ ] 贴地椭圆 Blob Shadow（不是实时阴影）

#### 我做

- [ ] 角色控制器：**左拇指摇杆 + 右拇指交互键，两者可同时按**
- [ ] 移动走**相机相对映射**（见下方坑）
- [ ] 手持系统：同时只能拿一样，头顶显示持有物
- [ ] 工位交互：进入触发范围 → 提示 → 按键取放
- [ ] 烤炉火候计时（三档：生 / 刚好 / 糊）
- [ ] 一个顾客走进来 → 点单（读 M0 那 20 张卡）→ 等待 → 判定 → 评价 → 离开
- [ ] 台词 UI（≤30 字上限，见 `docs/ai-customer-v1.md` §3 的论证）
- [ ] 金币结算 + 局末小结算面板

**M2 验收**：**掏出手机，30 秒内玩完一局**——顾客点了「双层牛肉不要洋葱五分熟」，
你做对了拿钱、做错了挨骂，局末看到结算。丑没关系，**流程不许缺一环**。

> ⚠ **斜 45° 的头号手感坑：摇杆方向映射。**
> 把摇杆的 `(x, y)` 直接当成世界的 `(x, z)`，玩家往上推、角色却斜着走——因为相机绕 Y 轴转了 45°。
> 正解是**先把输入向量按相机 yaw 旋转再喂给移动**（camera-relative movement）。
> 这行代码写不写对，决定这个游戏「手感对不对」，比后面所有美术都重要。

> ⚠ **双手同时输入要一开始就做对。** 别先做单手版再改——
> 同时处理两路触摸是输入层的架构问题，不是「加个按钮」。
> 右手是一个键还是两个（拿放 / 操作）在这里定，取决于烤炉与洗碗池要不要长按。

> ### ⛔ M2 出口：游戏名定死 + 提交软著
>
> **这是全计划最容易卡死的地方，必须在这里处理掉。**
>
> 软著审查约 **30 工作日**（普通流程，含来回补正现实要 6–12 周），而**软著证书上的软件名称
> 必须与最终上架的小游戏名称完全一致**——名字定错就要重新申请，直接推迟上线两三个月。
>
> 所以链条是：**游戏名定死 → 提软著 → 等 6–12 周 → 期间继续做 M3/M4/M5/M6 → 下证即可提审。**
> 在 M2 提交而不是上一版的 M3，是因为软著材料需要：
> - **源代码**：不足 60 页交全部，超过交前 30 + 后 30 页；每页 ≥50 行、无空行、A4 单面，
>   **页眉须印软件名称与版本号且与申请表一致**
> - **软件说明书 / 游戏设计文档**：含游戏规则与界面设计 → M2 的垂直切片刚好能出截图
> - 个人开发者另需《游戏自审自查报告》
>
> M2 结束时这些材料齐了；上一版排的 M3 反而更晚，且那时 UI 还在动，截图要重做。

---

### M3 · 压力落地（2–3 周）—— 核心卖点在这里成立

把 M1 在 Node 里调好的参数搬进真游戏，第一次验证「手忙脚乱」在真人手上是否成立。

- [ ] 多顾客并发 + 各自耐心倒计时
- [ ] 订单队列 UI：一眼能看清谁快超时了（横屏上方角落）
- [ ] 客流曲线按 M1 的难度表推进
- [ ] 工位排布调优——横屏的红利在这里用：靠**工位数量 + 工位间距离**双旋钮调压力
- [ ] 音效兜底：烤糊、超时、完成各一个提示音（没有音效的压力游戏压力少一半）

**M3 验收（两条，都要过）**：

1. **主观**：连玩 5 局，平均差评率 > 0 —— 一局不失误说明压力根本没调出来
2. **客观**：**模拟器预测 vs 真人实测的偏差**。M1 的理想玩家说「同时 4 单开始崩」，
   真人如果 2 单就崩，说明**操作摩擦**（走位、按键、看不清）比预期大一倍——
   这时该改的是操作和 UI，不是难度数值

> 判据 2 是分层架构的又一个红利：**它把「手感不好」这种主观抱怨变成了一个可量化的差值。**
> 没有 `sim.ts` 就只能靠「我觉得有点难」来调参。

---

### M4 · 混乱系统全套 + 打烊后循环（3–4 周）

M3 验证了压力机制成立，这里补齐两半：**营业中的混乱**（胡闹厨房侧）与
**打烊后的经营**（Burgie's 侧）。上一版漏了后半边，天数推进会推进到一个空房间。

#### 4a · 营业中：混乱源（GDD §3.2）

- [ ] 盘子有限 → 洗碗池 ← **优先做这个**。它不加新按钮，只让已有资源变紧，是性价比最高的混乱源
- [ ] 烤炉过火 → 烤糊 → 起火 → 灭火器
- [ ] 催单、差评、卫生检查
- [ ] 外卖骑手：独立倒计时的第二条队列（`GDD.md` §12.1）
- [ ] 难度曲线完整化：客流与订单复杂度按 M1 的表递增

> **每加一个混乱源过两道闸**：
> ① 落在 `GDD.md` §12 哪一段？营业中玩家一秒注意力不剩，需要阅读或思考的东西塞进去等于没写。
> ② 过「可决策」判据（见玩法基准节第三条）——玩家有没有「先 A 还是先 B」的选择？没有就是纯恶意。

#### 4b · 打烊后：经营循环（Burgie's 侧，上一版缺失）

- [ ] **一天 = 一关**：营业时段 3–4 分钟结束 → 强制打烊 → 进入结算
- [ ] 结算面板：营收、差评数、**三星评分**
- [ ] 顾客评价 → 小费 → 升级点数（学 Burgie's 的经济闭环，把 GDD §6 和 §7 接成一条链）
- [ ] 升级项：设备（烤炉更快）/ 布局（工位更近）/ 容量（盘子更多）
      —— 全都要**同步提高客流**，否则玩家越玩越闲（GDD §12.2 已识别的陷阱）
- [ ] 本地存档（`wx.setStorage`）——有天数推进就必须有存档

**M4 验收（两条）**：

1. 营业中你会手忙脚乱 —— 达不到就回 M1 的模拟器重扫参数，**不要在游戏里硬调**
2. 打烊后你想再来一天 —— 如果结算面板看完没有「明天试试升这个」的念头，经营循环就是空转

---

### M5 · 内容扩产（1–2 周，大半在等 batch）

M0 的 200 张已经验过判据，这里只是放大产量。

- [ ] 扩产到 2000–10000 张
- [ ] **自动敏感词过滤 + 抽样人工审核**（10000 张你不可能全读，但审核会看——
      需要一道机器过滤 + 随机抽 200 张人工过目的流程）
- [ ] 按天分包：首包带 300–500 张，其余按解锁天数下载（§2.4）
- [ ] 长线剧情 arc 挂上（见 `docs/ai-customer-v1.md` §6，V1 用预写剧情线，不调实时 API）

---

### M6 · 适配 · 性能 · 真机（2–3 周）

包体不是在这里才开始管——M0 定策略、M2/M3/M4 每个里程碑收尾各记一次数。这里做最后收口：

- [ ] 触摸手感调优（本机 headless 测不出，必须真机）
- [ ] **横屏适配**：覆盖 16:9 / 18:9 / 19.5:9 三档；
      刘海机横屏时缺口在**左右侧**，而摇杆与交互键正好也在左右侧，
      距边缘留 ≥50px 安全区 —— **这是全游戏最高频的两个触摸目标，必踩**
- [ ] 主包收进 3.8MB：字体 fontmin 提子集、音频转单声道 OGG、顾客卡按天分包
- [ ] 纹理全部转 ASTC（§2.7 定的格式）
- [ ] **性能三件事分开测**，别混成一句「卡不卡」：

| 测什么 | 用什么机器 | 看什么 |
|---|---|---|
| 帧率 | 中低端**安卓** | draw call、GPU 占用，低端 ≥28 |
| 内存 | 一台**旧 iPhone**（2G/3G RAM） | 会不会被系统杀（§2.5） |
| 帧时间尖刺 | 任意真机，长时间连玩 | GC 造成的间歇卡顿（§2.6） |

- [ ] 首屏加载压进 3000ms
- [ ] 上传体验版，拉朋友测（体验成员上限 15 人）

---

### M7 · 上架

- [ ] 软著下证（M2 就提交了，这里只是等）
- [ ] 类目申报、内容自查、《游戏自审自查报告》
- [ ] 提审、上线

---

## 六、开发流程：日常怎么跑

上一版只有一张「我做 / 你做」的表，但**没写怎么交接**——而每日摩擦比里程碑规划更影响推进速度。
这一节补齐。

### 6.1 分工边界

| 我做（服务器 · 纯代码） | 你做（Windows · 编辑器与真机） |
|---|---|
| `logic/` 全部代码 + 单元测试 | 编辑器摆场景、建 prefab、调材质 |
| `scripts/` 里的 Cocos 组件 `.ts` | 在 Inspector 里**拖引用**（我只能写 `@property` 声明，拖不了） |
| 逻辑梳理、状态机设计、性能优化 | 真机测手感、凭手感调数值 |
| 代码驱动的动画参数 | 视觉判断「这个动作好不好看」 |
| 内容管线（`pipeline/`） | 构建、上传、审核、软著 |

**我看不见你的编辑器。** 所以凡是「节点在场景里叫什么、挂在哪一层」这类信息，
要么按下面的命名约定，要么你告诉我。

### 6.2 命名约定（让我不用看编辑器也能写对代码）

| 类型 | 约定 | 例 |
|---|---|---|
| 工位节点 | `Station_<名>` | `Station_Fridge` · `Station_Grill` |
| 角色 prefab | `Prefab_<名>` | `Prefab_Customer` · `Prefab_Chef` |
| UI 根节点 | `UI_<名>` | `UI_OrderQueue` · `UI_Joystick` |
| 挂载点 | `Anchor_<名>` | `Anchor_Hand`（手持物挂这里） |

我按这套写 `find()` 和 `@property` 声明，你按这套建节点。**对不上的时候是你改名字，不是我改代码**
——因为代码有测试兜底，场景没有。

### 6.3 接口契约：`logic/API.md`

`logic/` 是我和你的 Cocos 组件之间唯一的接缝，接缝要有文档。

- 我每次改 `logic/` 的**公开导出**，同步改 `API.md`，且 commit message 打 `[API]` 前缀
- 你 pull 之后 Cocos 编译报错，第一件事看 `git log --oneline | grep API` 和 `API.md` 的 diff
- **铁律③的执行面**：组件里只允许调 `API.md` 上列出的东西。想要个新方法就跟我说，
  别在组件里自己写一份——那就是规则外泄的开始

### 6.4 每次开工 / 收工的固定动作

```bash
# 开工（30 秒，别跳过）
git pull
npx vitest run            # 逻辑层没坏，才值得开编辑器
# → 然后再启动 Cocos Creator

# 收工
git add -A && git commit -m "..." && git push
```

`vitest` 那一步是为了避免最糟的情况：**开了编辑器调半天，最后发现是我推的逻辑改动坏了**。
30 秒换掉半小时。

### 6.5 冲突处理：场景文件不 merge

Cocos 的 `.scene` / `.prefab` 是 JSON，但塞满节点 uuid，**冲突了几乎不可能手动 merge 对**。

| 情况 | 做法 |
|---|---|
| 场景 / prefab 冲突 | **不 merge。谁的版本新用谁的，另一方重做那一次改动。** 预防靠分工：这两类只有你碰 |
| `.ts` 冲突 | 正常 merge，有测试兜底 |
| `.meta` 文件 | ⚠ **必须进仓库**。它存 uuid 映射，删了引用全断，场景里所有拖好的引用变空 |
| `library/` `temp/` `build/` | 必须 `.gitignore`。本地重建产物，进仓库天天冲突 |

### 6.6 每周节奏

**业余项目最大的杀手不是技术难题，是失去节奏后再也回不来。**
里程碑是 2–4 周一个，中间太长，所以加一条硬要求：

> **每周至少产出一个能截图或录屏的变化。** 哪怕只是「角色手上多了个方块代表汉堡」。

看得见的进展是燃料。连续两周没有可视变化，就说明当前任务拆得太粗，要拆小。

### 6.7 卡住了怎么办

| 可以砍 | 不能砍 |
|---|---|
| 美术（几何体能一路撑到 M6） | 发布链路（M0 卡住就全盘卡住） |
| 音效（M4 再补，但压力游戏没音效会少一半张力） | 核心循环（接单→做→交付→反馈） |
| 动画打磨、粒子特效 | 存档（有天数推进就必须有） |
| 外卖、卫生检查等混乱源（M4 里挑两个做也成立） | 软著（时间线最长，砍了就上不了线） |

卡在某个技术点超过**两个晚上**就停下来问我，别自己硬啃——这是我在这个项目里最能帮上忙的地方。

### 6.8 里程碑闸门（Definition of Done）

**带着下面任何一条，不许进入下一个里程碑：**

- [ ] `npx vitest run` 有红灯
- [ ] 核心路径上留着 `TODO` / 写死的假数据
- [ ] 真机上跑不起来（开发者工具跑得起来不算数）
- [ ] 本里程碑的验收判据有一条没过
- [ ] 包体数字没记（M2 起每个里程碑收尾都要记一次，看趋势而不是最后才发现超了）

业余项目最常见的死法是「M2 还剩三个 bug，但我想做 M3 了」，然后债务滚到收不了场。

---

## 七、AI 的两个角色

**别混为一谈**——一个是帮你写代码的（第六节已展开），一个是游戏内容的原材料。

### 内容生产 = 分工用两个模型

| 用途 | 模型 | 理由 |
|---|---|---|
| **离线生成顾客卡** | **Claude Opus 5** + Batch API | 一次性创意工作，「好笑」是大模型才有的能力，这一环不要省。200 张约 $2.5 |
| **批量扩产 / 运行时实时**（若做） | **DeepSeek V4 Flash** | ① 国内 API，是唯一能进微信 request 白名单的那条路——`api.anthropic.com` 加不进去<br>② 1M 上下文，输入 1 元/M、输出 2 元/M，比 Opus 5 便宜约 35 倍<br>⚠ 峰谷定价：9–12 与 14–18 点双倍，**跑批放夜里** |

**V1 不需要任何运行时 API 调用**——顾客卡是离线生成的静态 JSON，打进包体，运行时纯本地抽卡。
将来若做「长线剧情动态推进」才需要实时调用，届时**必须自建服务器中转**（API key 不能进前端包体，
小游戏包可解包），那时才需要备案域名。

---

## 八、风险与预案

只列**真的可能发生、且发生了会卡住整条线**的。已经在正文里处理掉的（包体、内存、GC）不重复。

| 风险 | 触发信号 | 预案 |
|---|---|---|
| **M0 线 C 判据不过**（顾客卡不好笑） | 200 张读完你自己没兴趣 | **不等于放弃项目。** 先分清是「维度池选得差」还是「顾客本来就不是灵魂」。前者换池重生成（一天）；后者把重心移到混乱系统那条线——`GDD.md` §3.2 本来就是并列的第二个核心，游戏仍然成立，只是卖点从「AI 整活」变成「厨房失控」 |
| **分层验证不过**（§3.1 那条） | `assets/logic/` 的纯 TS 构建后没进产物 | 退到 tsconfig `outFile` 方案：`logic/` 放 assets 外，编译成单个 js 吐进 assets。代价是多一个构建步骤 + 调试行号对不上，但架构不用改 |
| **手上没有旧 iPhone** | M6 才发现测不了 iOS 内存 | ⚠ **现在就确认你有什么测试机。** 微信开发者工具的 iOS 模拟**测不出内存被杀**（§2.5）。真没有就得借一台，或接受「首发只保安卓」并在 M6 前想清楚 |
| **软著驳回或超时** | 补正通知 | 驳回多为材料格式问题（页眉、行数、页码），可补正重交；时间线因此要留缓冲，这也是排在 M2 而非更晚的原因 |
| **游戏名想改** | M2 之后突然想到更好的名字 | 软著提交后改名 = **重新申请、重新等 6–12 周**。所以 M2 那个闸门不是形式，定名时把 4 个候选一次性想清楚 |
| **你连续几周没时间** | 常态，不是意外 | ROADMAP 里维护「当前断点」一行：下次开工第一件事做什么。业余项目的恢复成本主要花在「我上次做到哪了」 |
| **换会话 / 我丢上下文** | 新开对话时我不记得项目状态 | **`ROADMAP.md` + changelog 是唯一的连续性载体，不能只存在对话里。** 按 CLAUDE.md 的节奏（约 10 项记一条，含文件清单）维护 |

---

## 九、需要核实的事（不拍脑袋）

**本轮已核实**（数据已并入第二节）：分包总量上限 = **16MB 红线 / 15MB 安全线**；
iOS 高性能模式免费、无 DAU 门槛、无主体限制；iOS 内存上限 1G（2G RAM）/ 1.4G（3G RAM）。

**仍未核实**：

- 纯免费无内购的小游戏上架**是否需要版号**（政策会变，需查当下要求）
- 个人主体小游戏的当前上架限制
- Cocos 3.8 **3D** 裁剪后的实际 `cocos-js` 体积（§2.4 用的是从 2D 数字外推的估值，**M0 实测替换**）

**2026-08-07 落盘时已核实 · Unison 配置位置与 ignore 语法**（原为待核实项）：

| 项 | 结果 |
|---|---|
| 主配置 | `/home/ly/.unison/sync.prf` —— 从 **WSL 端**运行 `unison sync`；root = `/home/ly` ↔ `ssh://ly@207.148.92.171//home/ly`；`prefer = newer` + `batch = true` |
| 另一份 | Windows 端 `C:\Users\12255\.unison\sync-server.prf`，源文件在服务器 `/home/ly/unison-setup/sync-server.prf`（root = `C:\Users\12255\server-sync` ↔ 服务器 `/home/ly`） |
| 版本 | unison 2.53.8，`/usr/local/bin/unison` |
| ignore 语法 | `ignore = Path <相对根的路径>` / `ignore = Name <glob>` / `ignore = Regex <正则>` |

⚠ **B1 落地时两份 `.prf` 都要改**，只改一份会漏。
⚠ ignore 用 **`Path project/kitchen-chaos/game/library`** 这种精确路径，**不要用 `Name library`**
——`Name` 是全局 glob，会误伤其它项目的同名目录。

---

## 十、第一周就做这些

三条线同时起跑。**顺序在每条线内部有依赖，线与线之间没有——不要互相等。**

### 线 A · 你（Windows / 微信后台）

> 状态不在这张表里维护，看「📍 当前断点」的逐步核对表 —— 两处都记就一定会有一处忘了改。
> 截至 2026-09-02：A1–A5 ✅ · A6 一半 · A7/A8 未开始。

| # | 做什么 | 卡住的话 |
|---|---|---|
| A1 | 等我把 Unison ignore 与 git repo 配好，再开始下一步 | 这是唯一需要等我的地方，半小时的事 |
| A2 | GitHub 建 private repo，clone 到 Windows | 复用 pangding-chong 的 SSH alias 方案 |
| A3 | 装 Cocos Creator 3.8 | — |
| A4 | 建 3D 空项目（**横屏 1280×720**）→ 构建 → 开发者工具 → **真机扫码** | ⭐ **本周最高优先级**。第一次做证书/AppID/构建配置容易卡 2–3 天，属正常 |
| A5 | 记下 `cocos-js` 与主包各多大 | 这个数字定了后面所有资源预算 |
| A6 | 注册小游戏 AppID；开通 iOS 高性能模式 | — |
| A7 | 验证 `assets/logic/` 的纯 TS 能被组件 import 且进构建产物 | 不过就走 §8 风险表的退路 |
| A8 | **盘一下你手上有哪些测试机**（安卓型号、有没有旧 iPhone） | 影响 M6 能不能测 iOS 内存，现在知道还来得及借 |

### 线 B · 我（逻辑层）

| # | 做什么 |
|---|---|
| B1 | ✅ **已完成** git repo + `.gitignore` 建好并首次提交；两份 Unison 配置已加 `ignore = Path project/kitchen-chaos`（**服务器侧的源，需你手动应用到两端**，见「当前断点」） |
| B2 | ✅ **已完成** 仓库根 `package.json` + `vitest.config.ts` + `tsconfig.json` + `pnpm-workspace.yaml`；`pnpm check` = 铁律① + typecheck + 测试 |
| B3 | ✅ **已完成** `types.ts` · `vec2.ts` · `collision.ts` + 52 个单测（含 `rotateY` 相机相对映射、圆-AABB 角落不误判、推出后不再重叠） |
| B4 | ✅ **已完成** `recipe.ts` · `order.ts` + 28 个单测（火候三档边界、required/banned 相交、错误项列全不短路） |
| B5 | ✅ **已完成** `tests/fixtures/customers.json` —— **21 张**手写卡，8 种 mood 全覆盖，含一条 3 章 arc |
| B6 | ✅ **已完成** `logic/API.md` 首版 |
| B7 | ✅ **已完成（2026-08-07）** 本方案落盘 `project/kitchen-chaos/ROADMAP.md`；同步改 `GDD.md`（引擎/美术结案、竖屏→横屏）、修 `docs/ai-customer-v1.md` 失效清单 |

### 线 C · 我（顾客卡验证）—— ⛔ 本周真正的重点

| # | 做什么 |
|---|---|
| C1 | ✅ **已完成** `pipeline/dimensions.json`（24×22×8×21 ≈ 8.9 万种组合，敏感值已剔除）+ `customer.schema.json`（draft 2020-12）+ `README.md`。schema 有 9 个测试守着，含 6 条「必须被拒」的活性守卫 |
| C2 | ⛔ **卡住：`ANTHROPIC_API_KEY` 未设置**。Opus 5 + Batch 生成 200 张（约 $2.5，跑批可放夜里）。脚本刻意还没写——等真要跑时再写，避免堆半截实现 |
| C3 | 按三条判据打分，**把 200 张原文交给你读** |

> **这一周结束时，最重要的产出不是「真机上跑起来了」，而是「你读完 200 张卡，
> 知道这个游戏的灵魂在不在」。** 前者是工程问题，卡住只是慢；后者是方向问题，错了就是三个月。

---

## 十一、验证判据总表

| 阶段 | 主判据（人来判断） | 硬判据（机器判断，不靠肉眼） |
|---|---|---|
| **M0** | 真机跑得起空场景；**200 张卡你自己想继续做** | 主包体积已记录 · `npx vitest run` 全绿 · `assets/logic/` 进了构建产物 |
| **M1** | — | `sim.ts` 输出难度曲线表，能回答「同时几单开始崩」 |
| **M2** | **30 秒玩完一局**，有始有终有结算 | vitest 覆盖订单判定边界（required/banned 相交、火候三档）· 铁律① `grep -rn "from 'cc'" game/assets/logic/` = 0 |
| **M3** | 连玩 5 局，平均差评率 > 0 | **模拟器预测 vs 真人实测的偏差** —— 偏差大说明该改操作和 UI，不是改数值 |
| **M4** | 营业中手忙脚乱；**打烊后你想再来一天** | 铁律② 10000 帧 `heapUsed` 平坦 · 包体趋势已记录 |
| **M5** | 抽样 200 张人工过目无越界 | 截图率 ≥15% · 重复感衰减点 ≥50 张 · 机制耦合率 ≥40%（`docs/ai-customer-v1.md` §7） |
| **M6** | 朋友能上手玩 | 低端安卓 ≥28fps · 旧 iPhone 不被杀 · 主包 ≤3.8MB · 首屏 ≤3000ms |

**三条通用的验证纪律**（来自 CLAUDE.md，在这个项目上都会碰到）：

1. **负向断言要先验锚点。** 「已无 XXX」这类结论，先确认取到的文件/节点真的存在——
   取错文件会让断言恒真、报全绿
2. **两个比对量若共享同一污染源，自洽 ≠ 正确。** 比如用同一份参数既跑模拟器又跑游戏，
   两边一致不能说明参数对，要比**不变量**
3. **性能结论只能来自真机。** headless 无 GPU、无真实内存压力、虚拟时间不推进动画
   （memory `headless-chromium-probe-limits`）。我能在服务器验证的只有 `logic/` 那三条铁律
   ——**这正是分层架构的价值所在**
